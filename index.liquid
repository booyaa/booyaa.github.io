extends: default.liquid

title: booyaa's boggy bloggy
route: blog
---

<ul class="blog-list">
  {% for post in posts %}
    <li>
      <a href="{{ post.path }}">{{ post.title }}</a>
    </li>
  {% endfor %}
</ul>
